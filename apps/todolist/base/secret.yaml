# Demo-only credentials, base64-encoded (not encrypted).
# In a real environment these values would be sealed with SOPS
# (see docs/setup.md) so plaintext secrets are never committed.
apiVersion: v1
kind: Secret
metadata:
  name: django-secrets
type: Opaque
data:
  SECRET_KEY: YWRtaW4=
  DB_USERNAME: YWRtaW4=
  DB_PASSWORD: cGFzc3dvcmQ=
